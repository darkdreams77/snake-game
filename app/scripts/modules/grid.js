define(['jquery', 'bootstrap'], function ($) {
    'use strict';

    var widthGrid   = 40,
        heightGrid  = 40,
        cellsGrid   = 10;

    var initGrid = function(widthGrid, heightGrid, cellsGrid) {
        var grid = document.getElementById('grid');
        console.log(grid);
    };

    console.log("game.js");
});